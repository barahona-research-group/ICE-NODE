
#PBS -lselect=1:ncpus=8:ompthreads=16:mem=48gb:ngpus=2:gpu_type=RTX6000

#PBS -lwalltime=24:0:0

# Load modules for any applications
module load mpi/intel-2018
module load cuda/10.2
module load cudnn/7.0

conda activate mimic3-snonet

# Change to the submission directory
cd $HOME/GP/MIMIC-SNONET

# Run program
# The program won't automatically use more than 1 core
# It has to be written with parallel capability

# The number of cores assigned to the job is available
# In the environment variable NCPUS or OMP_NUM_THREADS
$HOME/anaconda3/envs/mimic3-snonet/bin/python -m mimicnet.hpo -o $HOME/GP/ehr-data/mimic3-snonet-exp/dec07 -i $HOME/GP/ehr-data/mimic3-transforms -n 100

